{
  "name": "@borilla/object-pool",
  "version": "0.1.0",
  "description": "Reusable pool of objects to reduce garbage collection",
  "main": "lib/index.min.js",
  "scripts": {
    "minify": "uglifyjs src/index.js --compress --mangle-props --mangle-regex=\"/^_/\" --mangle toplevel --screw-ie8 --output lib/index.min.js",
    "lint": "eslint src/ test/",
    "unit-test": "mocha test/",
    "test": "npm run minify && npm run unit-test && npm run lint",
    "prepublish": "npm test"
  },
  "keywords": [
    "object",
    "pool",
    "garbage",
    "collection"
  ],
  "author": "borilla",
  "license": "MIT",
  "devDependencies": {
    "chai": "^3.5.0",
    "eslint": "^2.8.0",
    "mocha": "^2.4.5",
    "sinon": "^1.17.3",
    "sinon-chai": "^2.8.0",
    "uglify-js": "^2.6.2"
  }
}
