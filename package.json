{
  "name": "@borilla/object-pool",
  "version": "0.0.1",
  "description": "Reusable pool of objects to prevent excessive garbage collection",
  "main": "lib/index.js",
  "scripts": {
    "compile": "babel --presets es2015 -d lib/ src/",
    "lint": "eslint src/ test/",
    "pretest": "npm run compile",
    "test": "mocha test/",
    "posttest": "npm run lint",
    "prepublish": "npm test"
  },
  "keywords": [
    "object",
    "pool",
    "garbage",
    "collection"
  ],
  "author": "borilla",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.7.7",
    "babel-preset-es2015": "^6.6.0",
    "chai": "^3.5.0",
    "eslint": "^2.8.0",
    "mocha": "^2.4.5"
  }
}
